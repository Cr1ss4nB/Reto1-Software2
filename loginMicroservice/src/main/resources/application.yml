spring:
  application:
    name: login-service
  datasource:
    url: jdbc:mysql://localhost:3306/loginDB?useSSL=false&serverTimezone=UTC
    username: root
    password: Laura2004.
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
